apiVersion: v1
kind: Service
metadata:
  name: k8s-mcp-web
  namespace: sre-copilot
spec:
  selector:
    app: k8s-mcp-web
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
